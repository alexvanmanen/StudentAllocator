package nl.hsleiden.inf;

public enum Period {
	FIRST, SECOND, THIRD, FOURTH
}

// private int periodNumber;
// private List<Integer> validPeriodNumbers = Arrays.asList(new Integer[] { 1,
// 2, 3, 4 });
//
// public Period(int periodNumber){
// if(validPeriodNumbers.contains(periodNumber)){
// this.periodNumber = periodNumber;
// } else {
// throw new Error("Invalid period number "+ periodNumber);
// }
// }
